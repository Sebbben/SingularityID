# Database and db connection vars
POSTGRES_PASSWORD = " "
POSTGRES_USER = "postgres"
DATABASE_HOST = "db"
DATABASE_PORT = 5432
IDP_DB = "singularity_id" # Must reflect the db name in init.sql (all lower case)

APP_DB = "frontend_db"

# API vars
DEBUG = TRUE